import React from 'react';
import NavigationItem from './NavigationItem';

const Navigation = ({ contentLinks }) => {
    return <NavigationItem links={contentLinks} />;
};

export default Navigation;
